version: "3.7"
services:

  test:
    image: lint
    build:
      context: .
      dockerfile: dockerfile
      target: lint
    volumes:
    - ./local:/local:ro
    - ./model:/model:ro
    - ./secrets:/secrets:ro
