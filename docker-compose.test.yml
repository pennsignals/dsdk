version: "3.8"
services:

  test:
    image: test
    build:
      context: .
      dockerfile: dockerfile
      target: test
    volumes:
    - ./local:/local:ro
    - ./model:/model:ro
    - ./secrets:/secrets:ro
